import styled from "@emotion/styled";

export const DetailsList = styled.ul`
  list-style: none;
  padding: 0;
  margin-left: 0;
  margin-bottom: 24px;
  word-wrap: break-word;
  flex-wrap: wrap;
  display: flex;
  flex-direction: row;
  align-items: center;
  overflow: hidden;
  height: 42px;
`;
